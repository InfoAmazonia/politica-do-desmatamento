article.timeline-item
	.timeline-content
		.container
			.twelve.columns
				header
					h2 {{item.year}}
				section(ng-show="showInfo")
					p
						span(ng-repeat="content in item.contents") {{content.text}} 